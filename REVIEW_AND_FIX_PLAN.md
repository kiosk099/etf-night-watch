# ETF Night Watch — 동작 점검 및 수정 계획

작성일: 2026-09-19  
대상: `kiosk099/etf-night-watch` / `main`  
운영: Vercel Production

## 1. 현재 확인된 상태

- GitHub → Vercel 자동배포는 정상 동작한다.
- GitHub Actions의 단위 테스트와 문법 검사는 통과한다.
- 따라서 현재 핵심 문제는 **배포 실패가 아니라 런타임 데이터 수집/계산 방식**이다.
- 현재 운영 API는 한 번 계산할 때 Yahoo Finance 심볼을 **최대 39개** 조회하고, Naver에서도 기준일 + 7개 ETF 종가를 조회한다.
- 현재 테스트는 계산 함수 일부만 검증하며, 실제 외부 데이터의 지연·누락·휴장·프리마켓 상황은 검증하지 않는다.

## 2. 우선 확인된 문제

### P0-1. 프론트에서 CDN 캐시를 사실상 무력화하고 있음

현재 `index.html`은 다음 방식으로 API를 호출한다.

- `/api/estimate?ts=1789791891058`
- `cache: 'no-store'`
- 60초마다 자동 호출

반면 API는 다음 캐시 헤더를 보낸다.

- `s-maxage=45`
- `stale-while-revalidate=90`

문제는 매 요청마다 `ts`가 달라져 URL 자체가 새 요청이 되기 때문에 CDN 캐시를 거의 재사용하지 못한다는 점이다.

결과:

1. 사용자 1명만 켜 두어도 매분 새 Serverless 실행
2. 실행마다 Yahoo 약 39개 심볼 재조회
3. Naver 국내 종가 API도 반복 조회
4. Yahoo 429/지연/일부 심볼 실패 가능성 증가
5. 일부 데이터가 빠지면 ETF별 계산 품질이 갑자기 달라짐
6. 프리장처럼 자주 갱신할수록 오히려 불안정해질 수 있음

**최우선 수정 대상.**

---

### P0-2. Yahoo 요청량과 동시성이 과도함

현재 조회 대상은 환율·선물·대표 ETF·구성종목을 합쳐 최대 39개 심볼이다.

`loadCharts()`는 동시 요청 8개로 처리하고, 각 요청 timeout은 7.5초다.

Vercel Function `maxDuration`은 20초다.

외부 API가 느리거나 rate limit이 걸리면:

- 여러 wave의 요청이 누적될 수 있음
- 20초 실행 제한에 근접하거나 초과할 수 있음
- 일부 심볼만 null이 될 수 있음
- 결과적으로 ETF별 값이 랜덤하게 빠지는 것처럼 보일 수 있음

**필요 조치**

- 동일 시장 데이터의 서버 캐시 도입
- 국내 종가는 하루 1회 수준으로 캐시
- Yahoo 시세는 30~60초 단위 캐시
- 프론트에서 캐시 파괴 금지
- 필요하면 한 요청당 심볼 수 자체 축소

---

### P0-3. 장 마감 후·주말에는 '마지막 유효값' 대신 계산 실패가 발생함

`ratio()`는 target 시각과 마지막 데이터 간격이 45분을 넘으면 null을 반환한다.

따라서:

- 미국 선물 주말 휴장
- CME 일일 maintenance 구간
- Yahoo 데이터 지연
- 특정 종목의 애프터마켓 거래 부재

같은 상황에서 마지막 정상 가격이 있어도 계산을 폐기한다.

특히 `fxFactor`가 null이면 **7개 ETF 전체가 unusable**이 된다.

현재 구조는

> “시장에 새 가격이 없음”

과

> “데이터 수집에 실패함”

을 같은 종류의 실패처럼 처리한다.

**수정 방향**

- 마지막 유효 시세는 유지
- 데이터 age를 별도 표시
- `live / delayed / closed / stale / error` 상태를 구분
- 휴장 시에는 마지막 시장 시각 기준 추정가를 고정해서 표시
- 진짜 통신 실패와 정상적인 장 마감을 구분

---

## 3. 프리마켓 반영 문제

### P1-1. 프리장 데이터가 종목별로 고르게 존재하지 않음

Yahoo의 `includePrePost=true`를 사용하고 있지만 모든 종목이 프리마켓에서 동일하게 거래되는 것은 아니다.

특히:

- ETF
- 거래량이 적은 개별 종목
- 소형 우주 관련주

는 마지막 체결 시각이 서로 크게 다를 수 있다.

현재 `fairEquity()`는 15분 이상 오래된 종목에 선물 continuation을 붙이지만, 실제로는 다음이 혼재한다.

- 실제 프리마켓 체결값
- 수십 분 전 체결값 + 선물 보정
- QQQ/PAVE/ARKX 등 대표 프록시
- 구성종목 일부만 관측된 basket

따라서 화면상 모두 하나의 “현재 추정”으로 보이지만 신뢰 수준은 서로 다르다.

### 수정 방향

각 ETF에 아래 항목을 명확히 남긴다.

- 실제로 반영된 마지막 미국 시세 timestamp
- 직접 관측 구성비
- 프록시 대체 구성비
- 선물 보정 여부
- 데이터 age
- 프리마켓/정규장/애프터마켓 구분

화면에는 최소한:

- 실시간
- 부분 실시간
- 선물 보정
- 장 마감값 유지
- 데이터 오류

정도로 구분한다.

---

## 4. 종가 기준 문제

### P1-2. KRX 기준 거래일을 특정 ETF 하나(360200)로 결정함

현재 전체 ETF의 기준 거래일은 `ACE 미국S&P500(360200)`의 Naver 데이터 하나로 결정한다.

일반적인 거래일에는 맞을 가능성이 높지만 구조적으로는 취약하다.

- 특정 종목 API 장애
- 종목별 데이터 갱신 지연
- 거래정지/이상 데이터

가 발생하면 전체 기준일이 영향을 받는다.

### 수정 방향

1. KRX 기준 거래일을 별도 시장 캘린더 또는 다수 ETF 데이터로 검증
2. 각 ETF 종가를 같은 거래일 기준으로 독립 검증
3. 응답에 아래 값을 노출
   - `krxCloseDate`
   - `marketClose`
   - 실제 Naver 원본 날짜
   - 종가 source
4. 종가를 가져오지 못한 ETF만 실패시키고 전체 계산은 유지

---

## 5. 환율이 전체 계산의 단일 장애점임

현재 모든 ETF는 최종적으로:

`국내 종가 × 기초자산 factor × KRW/USD factor`

를 사용한다.

`KRW=X` 조회가 잠깐 실패하면 모든 ETF의 `dataOk`가 false가 된다.

### 수정 방향

- 환율 마지막 성공값 별도 캐시
- 환율 age 표시
- 일정 시간 내 값이면 delayed 상태로 계산 허용
- FX만 실패했을 때 전체 데이터까지 버리지 않음

---

## 6. 구성종목이 코드에 고정되어 있음

현재 구성종목 기준일:

- 미국테크TOP10 계열: 2026-09-11
- AI전력핵심인프라: 2026-09-11
- 미국우주테크: 2026-09-15

구성종목이 변경되면 계산은 정상 작동하는 것처럼 보이면서 실제 추정치는 점점 틀어질 수 있다.

이는 “사이트 장애”와는 별개지만 **값이 안 맞는 문제**의 원인이 될 수 있다.

### 수정 방향

- 최소한 holdings 기준일을 화면에서 계속 표시
- 일정 기간 이상 오래되면 warning
- 이후 자동/반자동 업데이트 방안 검토

---

## 7. 시장 세션 판정이 실제 거래 캘린더를 반영하지 않음

현재 `marketSession()`은 요일 + 미국 동부시간만 사용한다.

반영하지 않는 것:

- 미국 공휴일
- 조기 폐장
- 선물 별도 거래시간
- CME maintenance
- 종목별 extended-hours 가능 여부

따라서 화면에 “프리마켓/정규장”이라고 떠도 실제로는 휴장일 수 있다.

### 수정 방향

UI 세션명은 참고 정보로만 사용하고, 실제 계산 가능 여부는 **마지막 데이터 timestamp**를 기준으로 판단한다.

---

## 8. 현재 테스트의 한계

현재 `test.js`가 확인하는 것은:

- KRX 날짜 선택
- 종가 파싱
- `fairEquity()`의 일부 수학 계산

뿐이다.

아래 상황은 테스트하지 않는다.

- 프리마켓
- 정규장
- 애프터마켓
- 미국장 마감 이후
- 주말
- 한국/미국 공휴일 차이
- Yahoo 일부 심볼 누락
- Yahoo 429
- 환율 누락
- stale quote
- Naver 종가 누락
- 실제 API 응답 필드 변경

즉 **테스트 통과 = 운영 정상**이 아니다.

---

## 9. 수정 순서

### 1단계 — 데이터 흐름 안정화

- 프론트의 `?ts=Date.now()` 제거
- `no-store` 제거
- API CDN 캐시 실제 활용
- Yahoo/Naver 요청 캐시
- 외부 API 실패와 휴장 상태 분리

### 2단계 — 관측 가능성 추가

API 응답에 디버그 정보를 추가한다.

예:

```json
{
  "sourceTime": "...",
  "ageSec": 120,
  "source": "yahoo",
  "mode": "direct | future-adjusted | proxy | frozen",
  "error": null
}
```

개발 중에는 별도 `/api/diagnostics`를 두어 다음을 바로 확인할 수 있게 한다.

- KRX 기준일
- ETF별 종가
- Yahoo 심볼별 마지막 timestamp
- FX 마지막 timestamp
- 실패 심볼
- 사용한 factor
- 최종 계산식

### 3단계 — 프리마켓 계산 검증

대표 3종을 먼저 검증한다.

1. ACE 미국S&P500 — 단순 선물형
2. TIGER 미국필라델피아반도체나스닥 — ETF + 선물 보정형
3. TIGER 미국테크TOP10 — basket형

3종이 안정화된 뒤 나머지 ETF로 확대한다.

### 4단계 — 자동 검증

다음 시간대별 fixture를 테스트에 추가한다.

| 상황 | 기대 동작 |
|---|---|
| 한국장 마감 직후 | KRX 당일 종가 기준 |
| 미국 프리마켓 | 프리장 체결 + 선물 보정 |
| 미국 정규장 | 최신 미국 시세 반영 |
| 미국 애프터마켓 | extended-hours 반영 |
| 미국장 완전 종료 | 마지막 유효값 유지 + closed 표시 |
| 주말 | 금요일 마지막 유효값 유지 + stale/closed |
| FX 실패 | 최근 FX 캐시 사용 + delayed |
| 일부 종목 실패 | basket 일부 보정, 전체 API는 유지 |

---

## 10. 완료 기준

단순히 API가 HTTP 200을 반환하는 것을 정상으로 보지 않는다.

다음 조건을 모두 만족해야 한다.

1. 화면의 KRX 종가가 실제 최근 확정 종가와 일치
2. `krxCloseDate`가 정확
3. 프리마켓에서 미국 데이터가 실제로 갱신
4. 같은 시각 여러 번 새로고침해도 값이 불필요하게 흔들리지 않음
5. Yahoo 일부 실패에도 전체 화면이 죽지 않음
6. 주말/휴장에도 마지막 정상값을 볼 수 있음
7. 각 숫자가 어떤 시각·어떤 데이터에서 나온 것인지 추적 가능
8. 외부 API 호출량이 현재보다 크게 감소
9. GitHub Actions 테스트 통과
10. Vercel Production에서 실제 응답 검증 완료

## 11. 우선 결론

현재 문제는 계산식 하나의 오류라기보다 **데이터 수집 구조와 상태 처리 방식의 문제**가 더 크다.

특히 최우선 원인은 다음 세 가지다.

1. **프론트가 CDN 캐시를 무력화하여 외부 API를 과도하게 반복 호출함**
2. **39개 Yahoo 심볼 조회 구조가 rate limit·지연·부분 실패에 취약함**
3. **45분 freshness 조건 때문에 정상적인 휴장/데이터 공백도 실패로 처리함**

따라서 계산식을 더 복잡하게 만들기 전에 **데이터 수집 안정화 → 상태 가시화 → 프리장 계산 검증** 순서로 수정한다.
